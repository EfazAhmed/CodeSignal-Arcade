# You would like to write a function that takes integer numbers
# x, y, L, and R as parameters, and returns True if x^y lies within the
# interval (L, R] and False otherwise.

# Option 1 is the most optimal => (if L < x ** y <= R:)